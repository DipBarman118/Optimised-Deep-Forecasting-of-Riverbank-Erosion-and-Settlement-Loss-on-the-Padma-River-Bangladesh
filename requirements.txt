# Core — needed for Day 1 steps 1-4 (stub, dataset, metrics, baselines)
numpy>=1.24
scipy>=1.10
pyyaml>=6.0

# Data pipeline — B's side
earthengine-api>=0.1.380
pyproj>=3.5
rasterio>=1.3
matplotlib>=3.7

# Modelling — from Day 1 hour 16
# Colab and Kaggle already have torch; pinning it here can downgrade CUDA.
# Install locally only if you are not on a hosted runtime.
# torch>=2.0
# torchvision>=0.15

# Day 3, optional, hard 90-minute stop (playbook §8.2)
# openstl

# Day 5 — C's asset layer
# geopandas>=0.13
# shapely>=2.0
# rtree>=1.0
